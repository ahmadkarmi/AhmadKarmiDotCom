---
import { Image } from 'astro:assets';
import aljazeeraLogo from '../assets/clients/aljazeera.png';
import uulaLogo from '../assets/clients/uula.png';
import kfasLogo from '../assets/clients/kfas.png';
import nbkLogo from '../assets/clients/nbk.png';
import bleemsLogo from '../assets/clients/bleems.png';

const clients = [
  { name: 'Al Jazeera', logo: aljazeeraLogo, width: 200, height: 80 },
  { name: 'UULA', logo: uulaLogo, width: 160, height: 80 },
  { name: 'KFAS', logo: kfasLogo, width: 180, height: 80 },
  { name: 'NBK', logo: nbkLogo, width: 150, height: 80 },
  { name: 'Bleems', logo: bleemsLogo, width: 160, height: 80 },
];
---

<section class="section py-20 border-y border-border/40 bg-background-secondary/30 backdrop-blur-sm">
  <div class="container-custom">
    <div class="text-center mb-16">
      <p class="text-accent font-bold tracking-widest uppercase text-xs mb-3">Proven Trust</p>
      <h2 class="font-display text-3xl md:text-4xl text-foreground">
        Strategic Partnerships
      </h2>
    </div>
    
    <div class="flex flex-wrap items-center justify-center gap-12 md:gap-20">
      {clients.map((client) => (
        <div 
          class="group relative transition-all duration-300 hover:-translate-y-1"
          title={client.name}
        >
          <Image 
            src={client.logo}
            alt={client.name}
            width={client.width}
            height={client.height}
            class="h-20 md:h-32 w-auto object-contain filter grayscale opacity-40 group-hover:grayscale-0 group-hover:opacity-100 transition-all duration-500 ease-out"
            loading="lazy"
          />
        </div>
      ))}
    </div>
  </div>
</section>
